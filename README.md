# Testing

```
    npm init -y
    npm i -D jest || npm install --save-dev jest
    run the command “npm test” 
```


```
Creating Code coverage report: add in package.json file

"jest": {
     "collectCoverage":true
   }
```